# Backend Configuration
# Chọn 1 trong 2 cách dưới đây:

# CÁCH 1: Sử dụng Backend Local (http://localhost:8081)
# Bỏ comment dòng dưới để force sử dụng local backend
# VITE_USE_LOCAL_BACKEND=true

# CÁCH 2: Sử dụng Backend trên Ngrok (mặc định)
# Thay đổi URL ngrok của bạn ở đây
VITE_BACKEND_NGROK_URL=https://your-ngrok-url.ngrok-free.app

# Optional: URL API fallback
VITE_API_BASE_URL=http://localhost:8081

# Vite Mode (tự động set bởi Vite)
VITE_MODE=development